# Nginx 生产环境配置
# 用法：复制到 /etc/nginx/conf.d/ops.conf，然后 systemctl reload nginx

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 场景 A：无域名 / 未备案（临时使用 IP + 8080 端口）
# 员工访问：http://你的公网IP:8080
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
server {
    listen 8080;
    server_name _;

    # 关闭版本号显示，提升安全性
    server_tokens off;

    # 前端静态资源（Vue 构建产物）
    location / {
        root /opt/ops/web/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到 Node.js 后端
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 长连接优化
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # 健康检查端点
    location /health {
        proxy_pass http://localhost:3000;
        access_log off;
    }

    # 静态资源缓存（提升加载速度）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /opt/ops/web/dist;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 场景 B：已有域名且已备案（正式环境）
# 员工访问：http://你的域名.com 或 https://你的域名.com
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 使用此场景时，请将上方 server 块注释掉，并取消下方注释：

# server {
#     listen 80;
#     server_name 你的域名.com;
#
#     # HTTP 自动跳转 HTTPS（配置 SSL 后启用）
#     # return 301 https://$server_name$request_uri;
#
#     server_tokens off;
#
#     location / {
#         root /opt/ops/web/dist;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 30s;
#         proxy_read_timeout 30s;
#     }
#
#     location /health {
#         proxy_pass http://localhost:3000;
#         access_log off;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         root /opt/ops/web/dist;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# HTTPS 配置（配置 SSL 证书后启用）
# 推荐用 certbot 自动申请 Let's Encrypt 证书
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# server {
#     listen 443 ssl http2;
#     server_name 你的域名.com;
#
#     ssl_certificate /etc/letsencrypt/live/你的域名.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/你的域名.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     server_tokens off;
#
#     location / {
#         root /opt/ops/web/dist;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 30s;
#         proxy_read_timeout 30s;
#     }
#
#     location /health {
#         proxy_pass http://localhost:3000;
#         access_log off;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         root /opt/ops/web/dist;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
